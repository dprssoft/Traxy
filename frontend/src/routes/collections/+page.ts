// Stub: will be filled in during collections implementation.
// Loads collections from local SQLite.
export const load = async () => {
	return {
		collections: [] as import('$lib/types/collectionTypes').CollectionResponseDto[],
	};
};